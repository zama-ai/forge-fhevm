# Generated by `make generate` from the vendored host contracts. Suppress the diff (the
# bytecode blob alone is ~180 KB of hex) and keep these files out of language statistics.
# CI verifies them with `make check-generated`, which is what makes the blobs reviewable.
src/generated/** linguist-generated=true -diff
